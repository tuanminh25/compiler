// find the maximum of two integers

int main() {  
  int i = getInt();
  int j = getInt();
  if (i >= j)
    putIntLn(i);
  else
    putIntLn(j);
  // As mentioned in lectures, a return is automatically emitted by the supplied code for main.
   return 0;
}
